// Custom node renderers per node `type`. Phase 1 uses the default node;
// future iterations will register richer renderers here.
//
// TODO(IA-028): per-type custom nodes (agent badge, treaty layer chip, etc.)

export const NODE_TYPES = {} as const;
