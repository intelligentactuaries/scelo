// Workspace demo sample — moved to @scelo/core (the TUI offers the same
// bundled samples; one definition or they drift). This shim keeps the
// import site stable for HardDataWorkstation and anything else.

export {
  buildWorkspaceDemo,
  WORKSPACE_DEMO_READOUTS,
  WORKSPACE_DEMO_REFLEXIVE,
} from "@scelo/core";
