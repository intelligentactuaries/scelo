{
  "name": "@ia/web-online",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scelo Web — Bun server hosting the browser edition of the Scelo workbench.",
  "scripts": {
    "build": "bun run --cwd ../web build",
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
