<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Scelo IDE macOS entitlements.

  Required because we ship hardenedRuntime: true. The Electron renderer
  itself doesn't need these entitlements, but the main process spawns the
  bundled Python and R interpreters (via child_process.spawn), and the JIT
  + unsigned-executable-memory bits are needed by some of those runtimes
  (numpy/scipy load native .dylibs, R loads native CRAN libs).

  Keep this list minimal — anything we don't actually need increases the
  app's attack surface and tightens notarisation review.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.debugger</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
