% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_factors}
\alias{sc_factors}
\title{Annuity and assurance factors by age}
\usage{
sc_factors(basis = NULL, df = NULL, ..., i = 0.04, n = NULL, ages = NULL)
}
\arguments{
\item{basis}{A mortality basis (see \code{\link[=sc_qx]{sc_qx()}}).}

\item{df}{A data frame holding the basis, when \code{basis} is not the frame.}

\item{...}{Passed to \code{\link[=sc_qx]{sc_qx()}}: column names (\code{age}, \code{qx_col}, \code{lx},
\code{deaths}, \code{exposure_col}) and Makeham overrides (\code{A}, \code{B}, \code{c},
\code{multiplier}).}

\item{i}{Annual effective interest rate.}

\item{n}{Term in years (\code{NULL} for whole-life factors only).}

\item{ages}{\code{c(from, to)} age range.}
}
\value{
A \code{scelo_table}, blank where x + n runs past the table.
}
\description{
ax-due = Nx/Dx (annuity-due), ax = ax-due - 1, Ax = Mx/Dx (end-of-year
benefit) and, with a term \code{n}, ax-due:n = (Nx - Nx+n)/Dx,
A1x:n = (Mx - Mx+n)/Dx, nEx = Dx+n/Dx, Ax:n = A1x:n + nEx. The columns
are named as actuaries write them: \code{äx}, \code{ax}, \code{Ax}, \code{äx:n}, \verb{A¹x:n},
\code{nEx}, \code{Ax:n}.
}
\examples{
f <- sc_factors(i = 0.04, n = 10)
f[f$age == 40, ]
}
