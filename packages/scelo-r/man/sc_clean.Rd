% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{sc_clean}
\alias{sc_clean}
\title{Clean a data frame}
\usage{
sc_clean(df, ops = NULL, passes = NULL)
}
\arguments{
\item{df}{A data frame.}

\item{ops}{\code{NULL} / \code{"safe"} for the safe ops, \code{"all"} for everything, or
a character vector of op names (see \link{SC_ALL_OPS}).}

\item{passes}{Maximum passes (default 1, or 8 for \code{"all"}).}
}
\value{
A \code{scelo_table} of the cleaned data.
}
\description{
\code{sc_clean(df)} runs the safe ops once; \code{sc_clean(df, "all")} runs
everything until clean (at most 8 passes, or until the same plan comes
back twice). \code{ops} may also be a character vector of op names (aliases
accepted: "dedupe", "winsorize", "impute", "snake", "dates", "money" ...).
The returned table's notes list exactly what changed; nothing is printed.
}
\examples{
sc_clean(sc_sample("dirty"))
sc_notes(sc_clean(sc_sample("dirty"), "all"))
}
