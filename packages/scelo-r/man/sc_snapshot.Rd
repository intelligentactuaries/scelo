% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hard.R
\name{sc_snapshot}
\alias{sc_snapshot}
\alias{sc_restore}
\alias{sc_snapshots}
\title{Snapshots: a named copy of a frame on disk}
\usage{
sc_snapshot(df, name)

sc_restore(name)

sc_snapshots()
}
\arguments{
\item{df}{A data frame.}

\item{name}{Snapshot name.}
}
\value{
\code{sc_snapshot()} the file path (invisibly); \code{sc_restore()} the data
frame; \code{sc_snapshots()} a data frame of \code{name}, \code{at}, \code{rows}, \code{cols}, \code{hash}.
}
\description{
\code{sc_snapshot()} keeps a copy under \verb{~/.scelo/snapshots} (or
\verb{$SCELO_HOME/snapshots}) as RDS with a JSON sidecar (name, time, rows,
cols, hash) so a step can be undone later; \code{sc_restore()} loads one back
(RDS, or a CSV written by the Python package); \code{sc_snapshots()} lists them.
}
\examples{
Sys.setenv(SCELO_HOME = tempdir())
sc_snapshot(sc_sample("claims"), "claims0")
nrow(sc_restore("claims0"))
sc_snapshots()$name
}
