% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{sc_aggregate_loss}
\alias{sc_aggregate_loss}
\title{Aggregate loss distribution}
\usage{
sc_aggregate_loss(
  frequency = "poisson",
  severity = "lognormal",
  ...,
  method = "panjer",
  h = NULL,
  n = 4096L,
  n_sims = 100000L,
  seed = 42L,
  quantiles = c(0.5, 0.75, 0.9, 0.95, 0.99, 0.995)
)
}
\arguments{
\item{frequency}{poisson(\code{lam}) / negbin(\code{r}, \code{beta}) / binomial(\code{m}, \code{q}).}

\item{severity}{lognormal(\code{mu}, \code{sigma}) / gamma(\code{alpha}, \code{theta}) /
pareto(\code{alpha}, \code{theta}, Lomax) / exponential(\code{theta}) / weibull(\code{shape},
\code{scale}), or a numeric vector: an empirical severity sample.}

\item{...}{Frequency and severity parameters by name (\code{lam}, \code{r}, \code{beta},
\code{m}, \code{q}; \code{mu}, \code{sigma}, \code{alpha}, \code{theta}, \code{shape}, \code{scale}, \code{rate}).
They come before the options so that \verb{q = } and \verb{m = } can never be
mistaken for \code{quantiles} or \code{method}: give the options by full name.}

\item{method}{"panjer", "fft" or "mc".}

\item{h}{Lattice step; by default 20 mean aggregate losses spread over
\code{n} points.}

\item{n}{Lattice points.}

\item{n_sims}{Monte Carlo simulations (method "mc").}

\item{seed}{Seed for method "mc" (\code{NULL} leaves the RNG alone).}

\item{quantiles}{Probability levels for the VaR / TVaR rows.}
}
\value{
A \code{scelo_table} of \code{p}, \code{VaR}, \code{TVaR} with attributes \code{mean} and \code{sd}.
}
\description{
S = X1 + ... + XN for a named frequency and severity: mean, sd, and VaR /
TVaR at the \code{quantiles}, by Panjer recursion (exact on a lattice of step
\code{h}), FFT (same lattice, any frequency) or Monte Carlo.
}
\examples{
a <- sc_aggregate_loss("poisson", "lognormal", lam = 5, mu = 8, sigma = 1, h = 100, n = 1024)
a
attr(a, "mean")
}
