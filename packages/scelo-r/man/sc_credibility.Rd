% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{sc_credibility}
\alias{sc_credibility}
\alias{sc_buhlmann}
\title{Buhlmann-Straub credibility by group}
\usage{
sc_credibility(df, group = NULL, value = NULL, weight = NULL)

sc_buhlmann(df, group = NULL, value = NULL, weight = NULL)
}
\arguments{
\item{df}{A data frame.}

\item{group}{Group column (inferred by alias when \code{NULL}).}

\item{value}{Observation column (first numeric column when \code{NULL}).}

\item{weight}{Exposure column, or \code{NULL} for unit weights.}
}
\value{
A \code{scelo_table} with the group column, \code{n}, \code{weight}, \code{mean}, \code{Z},
\code{credibility_premium}, largest weight first; EPV / VHM / K in the basis.
}
\description{
Z = n / (n + K) with K = EPV / VHM, credibility premium Z * mean_g +
(1 - Z) * mu. \code{value} is the per-record observation (loss ratio, claim
count, ...), \code{weight} the exposure (1 when absent: plain Buhlmann). Groups
with one record get Z from the collective K like the rest. \code{sc_buhlmann()}
is an alias.
}
\examples{
df <- data.frame(group = rep(c("A", "B", "C"), c(6, 4, 2)),
                 lr = c(0.7, 0.8, 0.65, 0.75, 0.7, 0.72, 0.9, 0.95, 0.85, 0.88, 0.5, 0.55))
sc_credibility(df)
}
