% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_basicterm}
\alias{sc_basicterm}
\title{BasicTerm monthly projection}
\usage{
sc_basicterm(mp, assumptions = NULL, max_months = 1200)
}
\arguments{
\item{mp}{A model-point file (see \code{\link[=sc_model_points]{sc_model_points()}}).}

\item{assumptions}{A list as from \code{\link[=sc_basicterm_assumptions]{sc_basicterm_assumptions()}}; missing
entries take the defaults.}

\item{max_months}{Projection cap in months.}
}
\value{
A \code{scelo_table}: month, premiums, claims, expenses, net_cf,
discount, pv_net_cf.
}
\description{
Monthly term-life projection of a model-point file with lifelib
BasicTerm_ME semantics, in base R. Columns inferred: age_at_entry,
sum_assured, policy_term, and optionally sex, policy_count,
duration_mth, premium_pp. Mortality is Makeham q = clamp(A + B c^x, 0,
0.95) converted to monthly; level annual lapse; acquisition expense once
at issue, maintenance monthly; premiums default to SA q / 12 x loading
when the file has none. Returns the aggregate monthly cash flows with
PVs (attribute \code{pv}) and the break-even month (attribute
\code{break_even_month}, also in the notes).
}
\examples{
bt <- sc_basicterm(sc_sample("lifelib-mp"))
attr(bt, "pv")
}
