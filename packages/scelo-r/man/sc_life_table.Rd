% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_life_table}
\alias{sc_life_table}
\title{Life table}
\usage{
sc_life_table(basis = NULL, df = NULL, ..., ages = NULL, radix = 1e+05)
}
\arguments{
\item{basis}{A mortality basis (see \code{\link[=sc_qx]{sc_qx()}}).}

\item{df}{A data frame holding the basis, when \code{basis} is not the frame.}

\item{...}{Passed to \code{\link[=sc_qx]{sc_qx()}}: column names (\code{age}, \code{qx_col}, \code{lx},
\code{deaths}, \code{exposure_col}) and Makeham overrides (\code{A}, \code{B}, \code{c},
\code{multiplier}).}

\item{ages}{\code{c(from, to)} age range.}

\item{radix}{l(first age).}
}
\value{
A \code{scelo_table}.
}
\description{
Age, qx, px, lx, dx, Lx, Tx, ex. \code{sc_life_table()} uses the illustrative
Makeham basis; \code{sc_life_table(df)} your qx. The last age carries qx = 1
so Tx / ex are finite; Lx uses the uniform-deaths approximation
lx - dx / 2.
}
\examples{
lt <- sc_life_table()
head(lt)
sc_life_table(ages = c(60, 100), radix = 1000)
}
