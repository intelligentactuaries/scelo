% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hard.R
\name{sc_hard}
\alias{sc_hard}
\title{Stamp a table as hard data}
\usage{
sc_hard(table, title = NULL, basis = NULL, note = NULL, assumptions = NULL)
}
\arguments{
\item{table}{A data frame or \code{scelo_table}.}

\item{title}{Title to set (keeps the table's own when \code{NULL}).}

\item{basis}{Basis line to set.}

\item{note}{A note to append.}

\item{assumptions}{A named list recorded in the stamp.}
}
\value{
The \code{scelo_table}, stage \code{"hard"}, with a \code{provenance} attribute:
\code{hash}, \code{at}, \code{scelo}, \code{r}, \code{rows}, \code{columns}, \code{trail}, \code{assumptions}.
}
\description{
Content hash, timestamp, scelo and R versions, the last 12 audit entries
and an optional assumption set travel with the table from here on
(\code{sc_provenance()} reads them back, \code{sc_verify()} checks the hash).
}
\examples{
t <- sc_hard(data.frame(x = 1:3), title = "three rows", assumptions = list(i = 0.04))
sc_provenance(t)$hash
sc_verify(t)
}
