% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_tail}
\alias{sc_tail}
\title{Tail factor from the development pattern}
\usage{
sc_tail(factors, method = "exponential", n_fit = NULL, horizon = 50)
}
\arguments{
\item{factors}{Link ratios (for example \code{\link[=sc_ldf]{sc_ldf()}} or a result's \code{factors}).}

\item{method}{\code{"exponential"} or \code{"power"}.}

\item{n_fit}{Fit the last \code{n_fit} factors only.}

\item{horizon}{Periods to extrapolate.}
}
\value{
A single tail factor (1 when no factor exceeds 1).
}
\description{
Fit ln(f_k − 1) ~ a + b·k (\code{"exponential"}) or ~ a + b·ln k
(\code{"power"}, inverse power) to the factors above 1 and extrapolate;
returns the product of the extrapolated factors over \code{horizon} further
periods.
}
\examples{
sc_tail(c(1.5, 1.2, 1.1, 1.05, 1.02))
}
