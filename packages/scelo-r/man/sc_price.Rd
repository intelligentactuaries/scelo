% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines.R
\name{sc_price}
\alias{sc_price}
\title{Frequency (and severity) GLM pricing in one line}
\usage{
sc_price(
  data,
  formula,
  family = "poisson",
  offset = NULL,
  severity = NULL,
  by = NULL
)
}
\arguments{
\item{data}{A data frame or a file path.}

\item{formula}{The frequency formula (a string or a formula).}

\item{family}{Frequency family.}

\item{offset}{Exposure column entering as \code{log(offset)}.}

\item{severity}{Claim-amount column for the Gamma severity model (\code{NULL}: frequency only).}

\item{by}{Grouping column(s) for the frequency-severity summary in \code{attr(, "summary")}.}
}
\value{
A \code{scelo_table} of \code{factor}, \code{level}, \code{frequency} (and \code{severity},
\code{pure_premium}) with attributes \code{frequency}, \code{severity} (the models) and \code{summary}.
}
\description{
\code{sc_price(df, "claims ~ C(region) + age", offset = "exposure", severity = "paid")}
fits Poisson frequency and Gamma severity on the same right-hand side and
multiplies the relativities into a pure-premium table, with the models
as attributes.
}
\examples{
claims <- sc_sample("claims")
claims$n <- as.integer(claims$paid > 20000)
p <- sc_price(claims, "n ~ C(line)")
p$frequency
}
