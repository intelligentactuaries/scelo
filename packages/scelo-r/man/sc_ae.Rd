% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_ae}
\alias{sc_ae}
\title{Actual vs expected by age band}
\usage{
sc_ae(
  df,
  expected = NULL,
  age = NULL,
  deaths = NULL,
  exposure = NULL,
  band = 5,
  by = NULL
)
}
\arguments{
\item{df}{A data frame with age, deaths and exposure columns.}

\item{expected}{The expected mortality basis (see \code{\link[=sc_qx]{sc_qx()}}).}

\item{age, deaths, exposure}{Column names, inferred when \code{NULL}.}

\item{band}{Width of the age bands in years.}

\item{by}{A grouping column.}
}
\value{
A \code{scelo_table}.
}
\description{
Exposure, actual deaths, expected deaths = sum of exposure x qx(basis),
A/E and crude qx per \code{band}-year age band, with a total row. \code{expected}
defaults to the illustrative Makeham basis; pass a qx data frame, a named
qx vector, a \code{scelo_qx} or Makeham parameters. \code{by} adds a grouping
column.
}
\examples{
x <- data.frame(age = rep(60:69, each = 3), deaths = rep(c(1, 0, 2), 10), exposure = 100)
sc_ae(x)
}
