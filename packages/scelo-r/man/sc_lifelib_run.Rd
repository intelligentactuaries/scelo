% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifelib.R
\name{sc_lifelib_run}
\alias{sc_lifelib_run}
\title{Run a lifelib model on a model-point file}
\usage{
sc_lifelib_run(
  library = "basiclife",
  model = "BasicTerm_ME",
  model_points = NULL,
  space = "Projection",
  premium_from_file = TRUE
)
}
\arguments{
\item{library}{A lifelib library (see \code{\link[=sc_lifelib_models]{sc_lifelib_models()}}).}

\item{model}{A model in it.}

\item{model_points}{A policy / model-point file (mapped with
\code{\link[=sc_normalise_model_points]{sc_normalise_model_points()}}); \code{NULL} runs the model's own table.}

\item{space}{The modelx space holding \code{result_cf()} / \code{result_pv()}.}

\item{premium_from_file}{Use the file's \code{premium_pp} (when present)
instead of lifelib's premium table.}
}
\value{
A \code{scelo_table} of cash flows by \code{t}, with attributes \code{pv}
(data frame), \code{meta} (list) and \code{model} (the modelx model).
}
\description{
Reads \code{library/model} with modelx (copying the library into
\code{\link[=sc_lifelib_home]{sc_lifelib_home()}} the first time), sets the model-point table and
returns the aggregate cash flows (\code{result_cf()}), with the per-policy
present values (\code{result_pv()}, ids in the first column) in the \code{pv}
attribute, run metadata in
\code{meta} and the live modelx model in \code{model}. Works for the
model-point-driven models (basiclife BasicTerm_\emph{, savings CashValue_});
for other libraries it returns whatever \code{space.result_cf()} /
\code{result_pv()} give, or stops with a pointer to the model's own API.
}
\details{
lifelib has no R implementation: this needs the \code{reticulate} package
and a Python with lifelib and modelx (\verb{pip install "scelo[life]"}).
Without them the function stops and says so; \code{\link[=sc_basicterm]{sc_basicterm()}} is the
base-R projection that needs no Python.
}
\examples{
\dontrun{
cf <- sc_lifelib_run("basiclife", "BasicTerm_ME", sc_sample("lifelib-mp"))
attr(cf, "pv")
}
}
