% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing.R
\name{sc_design_matrix}
\alias{sc_design_matrix}
\title{Design matrix for the Scelo formula subset}
\usage{
sc_design_matrix(
  df,
  formula,
  drop_first = TRUE,
  levels = NULL,
  base = "frequent"
)
}
\arguments{
\item{df}{A data frame.}

\item{formula}{A string or formula.}

\item{drop_first}{Drop the base level of each categorical (\code{TRUE}).}

\item{levels}{Named list of the dummy levels to build per categorical
(used by \code{\link[=sc_predict]{sc_predict()}} to reproduce the training design).}

\item{base}{Reference level of each categorical: \code{"frequent"} (the most
common level, Scelo's default), \code{"first"} (alphabetical, as \code{\link[stats:glm]{stats::glm()}}
and statsmodels) or a named list \code{list(factor = level)}.}
}
\value{
A list: \code{y}, \code{X} (numeric matrix), \code{terms}, \code{levels}.
}
\description{
Parse \code{y ~ a + C(b) + c} into the response, a design matrix with an
\code{Intercept} column and the term names. \code{C()} marks a categorical (so do
character, factor and logical columns): it is one-hot encoded with the
most frequent level dropped as the base, columns named \code{b[level]}.
Numeric terms enter linearly.
}
\examples{
d <- sc_design_matrix(sc_sample("claims"), "paid ~ C(line) + age")
colnames(d$X)
}
