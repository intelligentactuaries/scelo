% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_reserve}
\alias{sc_reserve}
\title{Reserve summary in one line}
\usage{
sc_reserve(df, n_boot = 1000, seed = 42, ...)
}
\arguments{
\item{df}{A long claims data frame or a triangle.}

\item{n_boot}{Bootstrap simulations.}

\item{seed}{Seed for the bootstrap.}

\item{...}{Passed to \code{\link[=sc_triangle]{sc_triangle()}} (\code{origin}, \code{dev}, \code{value}, \code{payment}, ...).}
}
\value{
A \code{scelo_table} with one row per method: method, latest, ultimate,
ibnr, se, cv, p95, p99.
}
\description{
From a claims file (or a triangle) to chain ladder, Mack, Bornhuetter–Ferguson
(seeded with the chain-ladder ultimates) and the ODP bootstrap side by
side. The individual results are kept in \code{attr(, "results")}.
}
\examples{
sc_reserve(sc_sample("claims"), n_boot = 200)
}
