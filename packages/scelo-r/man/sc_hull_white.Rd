% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finance.R
\name{sc_hull_white}
\alias{sc_hull_white}
\title{Hull–White short-rate paths}
\usage{
sc_hull_white(
  r0 = 0.04,
  a = 0.1,
  sigma = 0.01,
  theta = NULL,
  horizon = 30,
  steps_per_year = 12,
  n_paths = 1000,
  seed = 42
)
}
\arguments{
\item{r0}{Initial short rate.}

\item{a}{Mean-reversion speed.}

\item{sigma}{Volatility.}

\item{theta}{Drift θ: \code{NULL} (a·r0), a number, or one value per year.}

\item{horizon}{Years.}

\item{steps_per_year}{Steps per year.}

\item{n_paths}{Number of paths.}

\item{seed}{Seed for R's RNG, or \code{NULL}.}
}
\value{
A \code{scelo_table} (t, mean, p5, p95, mean df) with attribute \code{paths}.
}
\description{
One-factor Hull–White / Vasicek: dr = (θ(t) − a·r)dt + σ dW, Euler on a
monthly grid. \code{theta} defaults to a·r0 (mean-reverting to r0); a vector
gives θ per year (the last value is held for any remaining years).
Returns per-step mean / p5 / p95 short rate and the mean discount factor
(the Monte-Carlo zero curve); the simulated paths (steps × paths) are in
\code{attr(, "paths")}. Uses R's RNG: \code{seed} makes a run reproducible (the
session's RNG state is restored afterwards).
}
\examples{
sc_hull_white(0.04, n_paths = 200, horizon = 3, seed = 1)
}
