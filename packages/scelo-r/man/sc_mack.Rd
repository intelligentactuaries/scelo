% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_mack}
\alias{sc_mack}
\title{Mack chain ladder}
\usage{
sc_mack(tri, average = "volume", tail_factor = 1)
}
\arguments{
\item{tri}{A triangle (see \code{\link[=sc_triangle]{sc_triangle()}}), numeric matrix or long claims file.}

\item{average}{\code{"volume"} (weighted), \code{"simple"} or \code{"regression"} link ratios.}

\item{tail_factor}{Tail factor beyond the last observed lag.}
}
\value{
A \link{scelo_reserving} result (method \code{"mack"}) with \code{se}, \code{cv} and
\code{detail$mse_by_origin}.
}
\description{
Mack (1993): the chain-ladder point estimate with the full MSE (process +
parameter error and the inter-origin covariance term).
σ̂²_k = Σ C(o,k)(C(o,k+1)/C(o,k) − f̂_k)² / (n_k − 1); the last σ² uses
Mack's extrapolation min(σ⁴_(k−1)/σ²_(k−2), σ²_(k−2), σ²_(k−1)).
Per-origin MSE and the total with the covariance term; SE = √MSE,
CV = SE / IBNR.
}
\examples{
sc_mack(sc_sample("claims"))
}
