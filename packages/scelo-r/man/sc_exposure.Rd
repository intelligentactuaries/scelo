% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_exposure}
\alias{sc_exposure}
\title{Central exposure by age}
\usage{
sc_exposure(
  df,
  start,
  end,
  birth = NULL,
  age_col = NULL,
  event = NULL,
  by_year = FALSE,
  age_basis = "last"
)
}
\arguments{
\item{df}{A data frame.}

\item{start, end}{Date column names (ISO dates, Date or POSIXt).}

\item{birth}{Date-of-birth column; when \code{NULL}, \code{age_col} (entry age) is
used.}

\item{age_col}{Entry-age column, inferred when \code{NULL} and \code{birth} is not
given.}

\item{event}{Event column (1 / TRUE / yes / dead / died / event / claim
count as deaths).}

\item{by_year}{Split the exposure by calendar year as well.}

\item{age_basis}{Age definition; only \code{"last"} (age last birthday) is
implemented.}
}
\value{
A \code{scelo_table}: age, (year), exposure, deaths, crude_qx.
}
\description{
Policy-years at risk by attained age (and calendar year), from start / end
dates, with deaths if \code{event} is given. Age is attained age from \code{birth}
(or the entry \code{age_col} advanced from \code{start}); each policy's time at
risk is split across the integer ages (age last birthday) it passes
through.
}
\examples{
sc_exposure(data.frame(start = "2020-01-01", end = "2022-01-01", age = 40.5, died = 1),
            "start", "end", event = "died")
}
