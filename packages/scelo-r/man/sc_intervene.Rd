% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swarm.R
\name{sc_intervene}
\alias{sc_intervene}
\title{Apply an intervention to a council run}
\usage{
sc_intervene(
  run_id,
  param,
  direction = "increase",
  magnitude = "small",
  rationale = "",
  recouncil = TRUE,
  subset = NULL,
  wait = TRUE
)
}
\arguments{
\item{run_id}{The run id.}

\item{param}{One of \link{SC_INTERVENTION_PARAMS}.}

\item{direction}{\code{"increase"} or \code{"decrease"}.}

\item{magnitude}{\code{"small"} or \code{"large"}.}

\item{rationale}{Free text recorded with the intervention.}

\item{recouncil}{Reconvene the council on the new forecast.}

\item{subset}{Agents for the reconvened council.}

\item{wait}{Poll until the new run completes.}
}
\value{
A \code{scelo_council} (or the new run id when \code{wait = FALSE}); the
WMTR payload when \code{recouncil = FALSE}.
}
\description{
Apply a WMTR intervention to a run: re-run the forecast and (by default)
reconvene the council on it.
}
\examples{
\dontrun{
sc_intervene("run_123", "pFamily", "increase", "large", recouncil = FALSE)
}
}
