% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_csm}
\alias{sc_csm}
\title{IFRS 17 CSM on the BasicTerm projection}
\usage{
sc_csm(mp, assumptions = NULL, ra = 0, ra_of = "claims")
}
\arguments{
\item{mp}{A model-point file.}

\item{assumptions}{A list as from \code{\link[=sc_basicterm_assumptions]{sc_basicterm_assumptions()}}.}

\item{ra}{Risk adjustment as a fraction of \code{ra_of}.}

\item{ra_of}{"claims" or "outflows".}
}
\value{
A \code{scelo_table} by year (coverage_units, csm_open, release,
accretion, csm_close) with attributes \code{csm0}, \code{loss_component}, \code{ra},
\code{pv}, \code{fcf}.
}
\description{
General-model CSM at initial recognition and its coverage-unit
roll-forward. Fulfilment cash flows = PV(claims + expenses) - PV(premiums)
\itemize{
\item RA, with RA a fraction \code{ra} of PV claims (\code{ra_of = "claims"}) or of PV
outflows. CSM0 = max(-FCF, 0); a negative value is the loss component.
The CSM accretes at the projection's discount rate and is released in
proportion to coverage units (in-force sum assured) each year.
}
}
\examples{
c <- sc_csm(sc_sample("lifelib-mp"), ra = 0.05)
attr(c, "csm0")
}
