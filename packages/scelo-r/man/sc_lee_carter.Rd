% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_lee_carter}
\alias{sc_lee_carter}
\title{Lee-Carter mortality projection}
\usage{
sc_lee_carter(
  df,
  year = NULL,
  age = NULL,
  rate = NULL,
  horizon = 10,
  headline_age = 65
)
}
\arguments{
\item{df}{A data frame with year, age and rate columns.}

\item{year, age, rate}{Column names, inferred when \code{NULL} (\code{rate}: qx,
else mx).}

\item{horizon}{Years to project.}

\item{headline_age}{Age reported in the forecast table (nearest
available).}
}
\value{
A \code{scelo_lee_carter}: a list with \code{ax}, \code{bx} (named by age),
\code{kt} (named by year), \code{drift}, \code{drift_se}, \code{forecast} (a
\code{scelo_table}) and \code{explained} (share of variance in the first
component).
}
\description{
SVD fit of log m(x, t) = a_x + b_x k_t on a long (year, age, qx or mx)
file, with constraints sum b_x = 1, sum k_t = 0, and a
random-walk-with-drift forecast of k_t. The forecast table gives the
projected rate at \code{headline_age} with a 95 percent interval (drift
uncertainty and innovation variance), plus the implied annual
improvement in the notes.
}
\examples{
d <- expand.grid(year = 1990:2019, age = 50:90)
d$qx <- exp(-9 + 0.09 * d$age - 0.015 * (d$year - 1990))
lc <- sc_lee_carter(d, horizon = 5)
lc$drift
}
