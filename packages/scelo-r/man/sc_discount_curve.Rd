% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finance.R
\name{sc_discount_curve}
\alias{sc_discount_curve}
\title{Discount-curve table}
\usage{
sc_discount_curve(curve = NULL, df = NULL, max_tenor = NULL)
}
\arguments{
\item{curve}{The curve (see Details), or \code{NULL} for a flat 4 \%.}

\item{df}{A data frame with tenor and rate columns (inferred by alias).}

\item{max_tenor}{Last tenor (default: 30, or the last quoted tenor if later).}
}
\value{
A \code{scelo_table}.
}
\description{
Tenor, zero rate, discount factor, 1y forward and annuity-certain a_n for
t = 1 ... \code{max_tenor}. \code{curve} is a flat rate, a named vector of rates by
tenor, a list of \code{c(tenor, rate)} pairs or a data frame with tenor + rate
columns (percent values are divided by 100); or pass the data frame as
\code{df}. Linear interpolation between quoted tenors, flat extrapolation
beyond the last (use \code{\link[=sc_smith_wilson]{sc_smith_wilson()}} for a UFR).
}
\examples{
sc_discount_curve(0.05, max_tenor = 10)
sc_discount_curve(c(`1` = 0.03, `5` = 0.04, `10` = 0.05), max_tenor = 12)
sc_discount_curve(data.frame(tenor = c(1, 2), rate = c(3, 4)), max_tenor = 2)
}
