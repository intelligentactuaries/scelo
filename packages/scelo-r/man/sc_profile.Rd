% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{sc_profile}
\alias{sc_profile}
\title{Per-column summary}
\usage{
sc_profile(df, columns = NULL)
}
\arguments{
\item{df}{A data frame.}

\item{columns}{Columns to profile (all by default).}
}
\value{
A \code{scelo_table}, one row per column.
}
\description{
Type, count, missing, unique, min / q1 / median / mean / q3 / max, Tukey
fences, outlier count, top values, date range: the numbers the IDE's
column headers show. \code{mixed} counts non-numeric cells in a number-typed
column ("6+"), which every numeric stat excludes.
}
\examples{
sc_profile(sc_sample("claims"))
}
