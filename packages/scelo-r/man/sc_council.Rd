% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swarm.R
\name{print.scelo_council}
\alias{print.scelo_council}
\alias{sc_council}
\title{Convene the council}
\usage{
\method{print}{scelo_council}(x, ...)

sc_council(
  scenario,
  subset = 32,
  society = 0,
  wait = TRUE,
  timeout = NULL,
  poll = 2,
  jurisdiction = "ZA",
  canon = NULL,
  fresh = FALSE,
  wmtr = TRUE,
  justify_all = FALSE,
  provider = NULL
)
}
\arguments{
\item{x}{A \code{scelo_council} to print.}

\item{...}{Passed to the summary table's print method.}

\item{scenario}{Scenario text.}

\item{subset}{Agents to convene.}

\item{society}{Society size polled alongside (0 = none).}

\item{wait}{Poll until complete (\code{TRUE}) or return the run id.}

\item{timeout}{Seconds to wait (default: 5 to 45 minutes, scaled to the run).}

\item{poll}{Seconds between polls.}

\item{jurisdiction}{Legal jurisdiction code.}

\item{canon}{Optional canon text.}

\item{fresh}{Bypass the server cache.}

\item{wmtr}{Inject the WMTR evidence.}

\item{justify_all}{Ask every agent for a cited justification.}

\item{provider}{Provider name for council, society and chat.}
}
\value{
A \code{scelo_council}: \code{run_id}, \code{summary} (a \code{scelo_table}), \code{votes},
\code{interventions}, \code{run} (the raw run), \code{trust}; or the run id.
}
\description{
Convene the stratified professional council on a scenario; returns the
\code{scelo_council} (or the run id when \code{wait = FALSE}). \code{subset} is the
number of agents (stratified across the 8 professions; at most 256);
\code{society} the size of the sentiment society to poll as well (0 skips it).
The run takes minutes with a cloud provider and longer on a local model.
}
\examples{
\dontrun{
cr <- sc_council("pension scheme with a weakening sponsor covenant", subset = 16)
cr$summary
}
}
