% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wmtr.R
\name{sc_wmtr}
\alias{sc_wmtr}
\title{WMTR survival forecast}
\usage{
sc_wmtr(scenario = NULL, ...)
}
\arguments{
\item{scenario}{Scenario text, a data frame (first row read as a scenario
row), a named list (one row), a \code{scelo_wmtr_params}, or \code{NULL} (the
defaults plus \code{...}).}

\item{...}{Parameter overrides (see \code{\link[=sc_wmtr_params]{sc_wmtr_params()}}).}
}
\value{
A \code{scelo_wmtr} (see \code{\link[=sc_run_wmtr]{sc_run_wmtr()}}).
}
\description{
Forecast an entity's survival: \code{sc_wmtr("pension scheme, sponsor covenant weakening")}, \code{sc_wmtr(df)} (a scenario row, e.g. from
\code{sc_sample("wmtr-scenarios")}), \code{sc_wmtr(shock = "severe", horizon = 50)}.
}
\details{
Returns a result whose table is the year-by-year mean / p10 / p25 / p75 /
p90 wealth, survival and the M, T, R means; outcome fractions, the
dominant outcome and the driver decomposition sit in the notes, in the
result and as attributes of the table. Parameters may be given by engine
name (\code{alphaM}) or column name (\code{alpha_m}).
}
\examples{
r <- sc_wmtr("rural village facing a severe drought", nPaths = 20)
r$dominant
sc_wmtr(sc_sample("wmtr-scenarios"), nPaths = 10)$params$shock
}
