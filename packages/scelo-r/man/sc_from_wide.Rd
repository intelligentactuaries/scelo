% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_from_wide}
\alias{sc_from_wide}
\title{Wrap an already-wide triangle}
\usage{
sc_from_wide(data, origins = NULL, cumulative = TRUE)
}
\arguments{
\item{data}{A numeric matrix, list of rows or data frame.}

\item{origins}{Origin labels, one per row.}

\item{cumulative}{Whether the figures are cumulative.}
}
\value{
A \code{scelo_table} triangle.
}
\description{
Rows are origins, columns are development lags 0 .. n−1. A matrix (or a
list of row vectors) takes its origins from \code{origins}, its rownames or
0 .. n−1; a data frame keeps its column labels when they are integers
(\code{"dev 1"} reads as 1) and otherwise relabels them 0 .. n−1, and a column
named like an origin (\code{origin}, \code{accident_year}, ...) supplies the origins.
}
\examples{
sc_from_wide(rbind(c(100, 150, 175), c(110, 160, NA), c(120, NA, NA)), origins = 2019:2021)
}
