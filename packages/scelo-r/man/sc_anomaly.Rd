% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate.R
\name{sc_anomaly}
\alias{sc_anomaly}
\title{Anomaly versus a seasonal baseline}
\usage{
sc_anomaly(
  series,
  dates = NULL,
  baseline = NULL,
  baseline_dates = NULL,
  by = "month"
)
}
\arguments{
\item{series}{Values: a named numeric vector, or a data frame.}

\item{dates}{Dates of \code{series} when it is not named.}

\item{baseline}{Baseline values (same forms as \code{series}); \code{NULL} uses the series.}

\item{baseline_dates}{Dates of \code{baseline} when it is not named.}

\item{by}{"month" or "day" (day of year).}
}
\value{
A numeric vector named by date.
}
\description{
The series minus its monthly (or day-of-year) climatology, taken from
\code{baseline} or from the series itself. \code{series} is a numeric vector with
dates as names (or \code{dates} given), or a data frame with a date column
(inferred by alias) and one numeric column.
}
\examples{
cl <- sc_sample("climate")
head(sc_anomaly(stats::setNames(cl$t2m_era5, cl$date)))
}
