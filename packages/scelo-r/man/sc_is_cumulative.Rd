% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_is_cumulative}
\alias{sc_is_cumulative}
\alias{sc_to_incremental}
\alias{sc_to_cumulative}
\title{Cumulative and incremental triangles}
\usage{
sc_is_cumulative(tri)

sc_to_incremental(tri)

sc_to_cumulative(tri)
}
\arguments{
\item{tri}{A triangle (see \code{\link[=sc_triangle]{sc_triangle()}}) or a numeric matrix.}
}
\value{
A logical for \code{sc_is_cumulative()}; a triangle otherwise.
}
\description{
\code{sc_is_cumulative()} reads the triangle's \code{cumulative} attribute, or for a
plain matrix checks that no row ever decreases. \code{sc_to_incremental()} and
\code{sc_to_cumulative()} convert (and return the input unchanged when it is
already in that form).
}
\examples{
tri <- sc_triangle(sc_sample("claims"))
sc_is_cumulative(tri)
sc_to_incremental(tri)
}
