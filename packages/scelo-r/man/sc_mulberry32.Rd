% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wmtr.R
\name{sc_mulberry32}
\alias{sc_mulberry32}
\title{Mulberry32: the swarm's seedable RNG}
\usage{
sc_mulberry32(seed)
}
\arguments{
\item{seed}{An integer seed (reduced modulo 2^32 like JavaScript's \verb{>>> 0}).}
}
\value{
A function \code{rand(n = 1)} returning \code{n} draws and advancing the state.
}
\description{
The same seed gives the same stream as Scelo IDE and the swarm engine
(\code{apps/swarm/src/shared/wmtr.ts}), bit for bit. The closure returns \code{n}
uniforms in \verb{[0, 1)} per call; the Monte Carlo consumes them one at a
time in the engine's order.
}
\examples{
r <- sc_mulberry32(42)
r()      # 0.6011037519201636
r(3)
}
