% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workspace.R
\name{sc_active_subspace}
\alias{sc_active_subspace}
\title{Active subspace of a readout}
\usage{
sc_active_subspace(df, readout, drivers = NULL, max_rows = 20000)
}
\arguments{
\item{df}{A data frame.}

\item{readout}{The output column.}

\item{drivers}{Driver columns (every other numeric column by default).}

\item{max_rows}{Thin the frame to at most this many rows.}
}
\value{
A \code{scelo_table} with \code{direction}, \code{eigenvalue}, \code{sensitivity_share},
\code{variance_share}, \code{name}, \code{loadings} (up to six directions); attributes
\code{rank}, \code{participation_ratio}, \code{surrogate_r2}, \code{sensitivity_spectrum},
\code{variance_spectrum}, \code{directions} (the eigenvector matrix).
}
\description{
Eigen-directions of the gradient covariance C = E(grad f grad f') of a
linear-quadratic surrogate of the readout. Reports each direction's
sensitivity share, input-variance share and named loadings: a direction
can carry most of the decision and almost none of the variance (the
workspace signature).
}
\examples{
ws <- sc_sample("workspace-demo")
a <- sc_active_subspace(ws, "annuity_60", drivers = c("mortality_trend", "cohort_effect", "smoking_index", "crude_rate"))
attr(a, "rank")
}
