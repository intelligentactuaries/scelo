% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_triangle}
\alias{sc_triangle}
\title{Origin × development triangle from a long claims file}
\usage{
sc_triangle(
  df,
  origin = NULL,
  dev = NULL,
  value = NULL,
  payment = NULL,
  cumulative = TRUE,
  incremental_input = TRUE
)
}
\arguments{
\item{df}{A long claims data frame (or an existing triangle).}

\item{origin}{Origin-period column (inferred: origin, accident_year, ay, ...).}

\item{dev}{Development-lag column (inferred: dev, lag, development_period, ...).}

\item{value}{Amount column (inferred: paid, incurred, amount, ...).}

\item{payment}{Calendar-period column used when there is no lag column.}

\item{cumulative}{Return a cumulative (\code{TRUE}) or incremental triangle.}

\item{incremental_input}{Whether the input rows are incremental amounts.}
}
\value{
A \code{scelo_table} triangle.
}
\description{
Rows are summed per (origin, lag), the columns inferred by alias when not
given. \code{dev} is an integer lag column; or give \code{payment} (a calendar
period) and lag = payment − origin. Input rows are INCREMENTAL amounts by
default and are accumulated along development (\code{cumulative = TRUE}); pass
\code{incremental_input = FALSE} when the file already holds cumulative
figures. Cells beyond the latest observed diagonal are \code{NA}; cells inside
it with no claims are 0.
}
\details{
The result is a \code{scelo_table} with the origins as rownames and the
development lags as column names (\code{"0"}, \code{"1"}, ...), carrying the
attributes \code{scelo_triangle = TRUE} and \code{cumulative}. A triangle passed in
is returned unchanged.
}
\examples{
tri <- sc_triangle(sc_sample("claims"))
tri
sc_triangle(sc_sample("claims"), cumulative = FALSE)
}
