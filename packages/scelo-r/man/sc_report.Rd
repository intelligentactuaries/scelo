% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hard.R
\name{sc_report}
\alias{sc_report}
\title{Assemble a board pack}
\usage{
sc_report(
  ...,
  title = "Board pack",
  to = NULL,
  summary = NULL,
  author = NULL,
  stamp = TRUE
)
}
\arguments{
\item{...}{Tables (\code{scelo_table}, data frames, or Scelo results carrying a
\verb{$table}) and strings (Markdown sections).}

\item{title}{Report title.}

\item{to}{File to write (\code{.md}, or \code{.html} / \code{.htm} for HTML).}

\item{summary}{Executive summary text.}

\item{author}{Author line.}

\item{stamp}{Stamp unstamped tables with \code{\link[=sc_hard]{sc_hard()}}.}
}
\value{
The Markdown text (invisibly when \code{to} is given).
}
\description{
A report from tables (and free-text sections given as strings): title,
generated line, optional executive summary, every table with its basis,
notes and provenance line, the audit trail and a footer. Returns
Markdown; writes HTML when \code{to} ends in \code{.html}. Unstamped tables are
stamped first when \code{stamp} is \code{TRUE}.
}
\examples{
md <- sc_report(sc_hard(data.frame(x = 1:3), title = "three rows"), "## Note\n\ntext", title = "Pack", summary = "ok")
cat(substr(md, 1, 60))
}
