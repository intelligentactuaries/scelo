% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_premium}
\alias{sc_premium}
\title{Net premium grid}
\usage{
sc_premium(
  basis = NULL,
  df = NULL,
  ...,
  i = 0.04,
  product = "term",
  ages = NULL,
  step = 5,
  terms = c(10, 15, 20, 25, 30),
  per = 1000
)
}
\arguments{
\item{basis}{A mortality basis (see \code{\link[=sc_qx]{sc_qx()}}).}

\item{df}{A data frame holding the basis, when \code{basis} is not the frame.}

\item{...}{Passed to \code{\link[=sc_qx]{sc_qx()}}: column names (\code{age}, \code{qx_col}, \code{lx},
\code{deaths}, \code{exposure_col}) and Makeham overrides (\code{A}, \code{B}, \code{c},
\code{multiplier}).}

\item{i}{Annual effective interest rate.}

\item{product}{\code{"term"}, \code{"endowment"} or \code{"whole-life"}.}

\item{ages}{\code{c(from, to)} for the age grid (default 20 to 65).}

\item{step}{Age step of the grid.}

\item{terms}{Policy terms (columns \code{n=10}, ...); ignored for whole-life.}

\item{per}{Sum assured the premium is quoted per.}
}
\value{
A \code{scelo_table}.
}
\description{
Annual net (equivalence-principle) premium per \code{per} of sum assured: an
age x term grid, payable in advance. \code{product} = term / endowment /
whole-life. P = per A / a-due with no expense loading and no margin: a
pure risk premium.
}
\examples{
sc_premium(i = 0.04, product = "term")
sc_premium(product = "whole-life", ages = c(30, 60), step = 10)
}
