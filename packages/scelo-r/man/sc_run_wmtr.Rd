% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wmtr.R
\name{sc_run_wmtr}
\alias{sc_run_wmtr}
\alias{print.scelo_wmtr}
\title{Run the WMTR Monte Carlo}
\usage{
sc_run_wmtr(params)

\method{print}{scelo_wmtr}(x, ...)
}
\arguments{
\item{params}{A \code{scelo_wmtr_params} (or a plain list of the same fields).}

\item{x}{A \code{scelo_wmtr} to print.}

\item{...}{Passed to the table's print method.}
}
\value{
A list of class \code{scelo_wmtr}: \code{params}, \code{paths} (one list per
path with \code{w}, \code{m}, \code{t}, \code{r}, \code{surv}, \code{outcome}), \code{table} (a
\code{scelo_table}: \code{year}, \code{mean_W}, \code{p10_W}, \code{p25_W}, \code{p75_W}, \code{p90_W},
\code{survival}, \code{mean_M}, \code{mean_T}, \code{mean_R}), \code{outcome_fractions},
\code{dominant}, \code{w0}, \code{drivers}, \code{survival} (at the horizon).
}
\description{
Run the forecast for explicit parameters (see \code{\link[=sc_wmtr]{sc_wmtr()}} for the
convenient form). The result holds the paths, the year-by-year table,
the outcome fractions, the dominant outcome and the driver decomposition.
}
\examples{
r <- sc_run_wmtr(sc_wmtr_params(nPaths = 20, horizon = 10))
r$outcome_fractions
}
