% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_ldf}
\alias{sc_ldf}
\alias{sc_cdf}
\title{Link ratios and cumulative development factors}
\usage{
sc_ldf(tri, average = "volume", n_periods = NULL, tail_factor = 1)

sc_cdf(tri, average = "volume", n_periods = NULL, tail_factor = 1)
}
\arguments{
\item{tri}{A triangle, numeric matrix or long claims file.}

\item{average}{\code{"volume"} (weighted), \code{"simple"} or \code{"regression"}.}

\item{n_periods}{Use only the latest \code{n_periods} origins per factor.}

\item{tail_factor}{Tail factor beyond the last observed lag.}
}
\value{
A named numeric vector (\verb{0→1}, ..., \code{tail} for \code{sc_ldf()}; the lags for \code{sc_cdf()}).
}
\description{
\code{sc_ldf()} gives the selected link ratios f_k (\code{average} = volume /
simple / regression; \code{n_periods} = latest n origins only) with a tail;
\code{sc_cdf()} the cumulative factors to ultimate, one per development lag.
}
\examples{
tri <- sc_triangle(sc_sample("claims"))
sc_ldf(tri)
sc_cdf(tri)
}
