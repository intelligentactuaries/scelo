% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{sc_fix_encoding}
\alias{sc_fix_encoding}
\alias{sc_trim}
\alias{sc_collapse_ws}
\alias{sc_missing_tokens}
\alias{sc_parse_numbers}
\alias{sc_parse_dates}
\alias{sc_booleans}
\alias{sc_sentinels}
\alias{sc_coerce_numeric}
\alias{sc_recode}
\alias{sc_future_years}
\alias{sc_impute}
\alias{sc_cap_outliers}
\alias{sc_dedupe}
\alias{sc_drop_empty}
\alias{sc_drop_constant}
\alias{sc_lowercase}
\alias{sc_snake_names}
\title{Individual cleaning ops}
\usage{
sc_fix_encoding(df, columns = NULL)

sc_trim(df, columns = NULL)

sc_collapse_ws(df, columns = NULL)

sc_missing_tokens(df, columns = NULL, tokens = NULL)

sc_parse_numbers(df, columns = NULL)

sc_parse_dates(df, columns = NULL, day_first = NULL)

sc_booleans(df, columns = NULL)

sc_sentinels(df, columns = NULL, values = NULL)

sc_coerce_numeric(df, columns = NULL)

sc_recode(df, column, from, to)

sc_future_years(df, columns = NULL, max_year = NULL)

sc_impute(
  df,
  columns = NULL,
  strategy = "auto",
  indicator = TRUE,
  value = NULL
)

sc_cap_outliers(df, columns = NULL, k = 1.5)

sc_dedupe(df, columns = NULL)

sc_drop_empty(df, threshold = 0.95)

sc_drop_constant(df)

sc_lowercase(df, columns = NULL)

sc_snake_names(df)
}
\arguments{
\item{df}{A data frame.}

\item{columns}{Columns to touch; \code{NULL} picks them by the op's rule.}

\item{tokens}{Missing markers to null (default \link{SC_MISSING_TOKENS}).}

\item{day_first}{Day-first reading for ambiguous dates; \code{NULL} infers it per column.}

\item{values}{Sentinel values to null; \code{NULL} applies the IDE's rule.}

\item{column}{One column name.}

\item{from, to}{The exact value to replace and its replacement.}

\item{max_year}{Years after this are nulled (default: this year).}

\item{strategy}{\code{auto} / \code{median} / \code{mean} / \code{mode} / \code{value}.}

\item{indicator}{Add the \verb{was_missing_<col>} indicator column.}

\item{value}{The fill for \code{strategy = "value"}.}

\item{k}{Tukey fence multiplier (1.5).}

\item{threshold}{Drop columns with more than this share missing.}
}
\value{
A data frame (never a \code{scelo_table}; \code{\link[=sc_clean]{sc_clean()}} wraps the plan).
}
\description{
Each op is one row of the cleaning banner, as a function from a data
frame to a data frame. \code{sc_fix_encoding()} repairs UTF-8 / Latin-1
mojibake ("Ã©" becomes "é"), NBSP to space, and drops BOM,
zero-width and soft-hyphen characters; \code{sc_trim()} strips leading /
trailing whitespace; \code{sc_collapse_ws()} collapses runs of internal
whitespace to one space; \code{sc_missing_tokens()} nulls the missing markers
("N/A", "?", "-", "TBD", "#N/A", "null", ... case-insensitive, trimmed);
\code{sc_parse_numbers()} turns money / percent / thousands-separated strings
into numbers (columns default to those at least 80 \% parseable);
\code{sc_parse_dates()} parses mixed-format date strings into \code{Date} (day-first
inferred per column from the unambiguous cells; time of day is dropped,
see \code{\link[=sc_parse_date]{sc_parse_date()}} to keep it); \code{sc_booleans()} standardises yes / no /
Y / N / true / false / on / off into a logical column; \code{sc_sentinels()}
nulls legacy numeric sentinels (-999, 9999, -1 ...) when they sit at
least 5 IQR outside the column's body and recur at least 3 times;
\code{sc_coerce_numeric()} turns number-typed columns with string residue
numeric, keeping the numeric prefix ("6+" becomes 6) and nulling the
digit-free rest; \code{sc_recode()} replaces one exact value in one column;
\code{sc_future_years()} nulls integer years after \code{max_year} in year-named
numeric columns; \code{sc_impute()} fills missing cells (numeric: median,
categorical: mode) with a \verb{was_missing_<col>} indicator inserted right
after the column; \code{sc_cap_outliers()} winsorises numeric columns to the
Tukey fences; \code{sc_dedupe()} drops exact duplicate rows; \code{sc_drop_empty()}
drops columns more than \code{threshold} missing; \code{sc_drop_constant()} drops
single-valued columns; \code{sc_lowercase()} lower-cases string columns whose
top values differ only by case; \code{sc_snake_names()} snake_cases the headers
(abandoned entirely on any collision).
}
\details{
\code{sc_impute()} follows the IDE's refusal rules in \code{auto} mode: nothing
missing, fewer than 4 known values, over 95 \% missing, a date column, an
identifier-like column (unique > max(20, 2 \% of present)) or no value
common enough to be a mode (< 2 \%) are skipped rather than invented.
\code{sc_cap_outliers()} skips year columns (a calendar fact, not a
distribution), identifier-like columns (all distinct and id-named) and
columns with no spread when no columns are named. Strictly numeric
strings are numbers to Scelo and are never treated as text by these ops.
}
\examples{
d <- sc_sample("dirty")
head(sc_parse_numbers(d)$premium_zar)
sc_snake_names(d)[1:2, ]
}
