% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing.R
\name{sc_glm}
\alias{sc_glm}
\title{Fit a pricing GLM}
\usage{
sc_glm(
  df,
  formula,
  family = "poisson",
  offset = NULL,
  weights = NULL,
  link = NULL,
  power = 1.5,
  base = "frequent"
)
}
\arguments{
\item{df}{A data frame.}

\item{formula}{\code{y ~ a + C(b) + c} (string or formula): \code{C()} marks a
categorical whose base level is its most frequent level.}

\item{family}{poisson / gamma / gaussian / binomial / tweedie / inverse_gaussian.}

\item{offset}{Exposure column (enters as log(offset)).}

\item{weights}{Prior-weight column.}

\item{link}{Link override: log / identity / logit.}

\item{power}{Tweedie variance power (needs the statmod package).}

\item{base}{Reference level of each categorical: \code{"frequent"} (most common,
the default), \code{"first"} (alphabetical, as \code{\link[stats:glm]{stats::glm()}}) or a named list.}
}
\value{
A \code{scelo_glm}: \code{family}, \code{link}, \code{formula}, \code{coef} (a
\code{scelo_table} of term / estimate / std_err / z / p_value / exp),
\code{params}, \code{cov}, \code{deviance}, \code{null_deviance}, \code{aic}, \code{n}, \code{df_resid},
\code{dispersion}, \code{fitted}, \code{engine}, \code{terms}, \code{levels}.
}
\description{
\code{sc_glm(df, "claims ~ C(region) + age", "poisson", offset = "exposure")}.
Families: poisson, gamma, gaussian, binomial, tweedie (\code{power}),
inverse_gaussian; default links log / log / identity / logit / log / log.
\code{offset} is a column entering as log(offset) (exposure); \code{weights} prior
weights. Rows with a missing response or term, a non-positive offset (or,
for gamma / inverse_gaussian, a non-positive response) are dropped.
}
\examples{
claims <- sc_sample("claims")
m <- sc_glm(claims, "paid ~ C(line) + age", "gamma")
m
sc_relativities(m)
}
