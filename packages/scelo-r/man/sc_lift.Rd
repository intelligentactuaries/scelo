% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing.R
\name{sc_lift}
\alias{sc_lift}
\title{Lift chart}
\usage{
sc_lift(actual, predicted, bins = 10L, exposure = NULL)
}
\arguments{
\item{actual}{Observed values.}

\item{predicted}{Model predictions.}

\item{bins}{Number of bands.}

\item{exposure}{Weights (1 per row when \code{NULL}).}
}
\value{
A \code{scelo_table} of \code{band}, \code{exposure}, \code{actual}, \code{predicted},
\code{lift}, \code{a/e}.
}
\description{
Sort by prediction into \code{bins} equal-exposure bands and compare the mean
actual with the mean predicted per band; the Gini coefficient is in the
basis.
}
\examples{
claims <- sc_sample("claims")
m <- sc_glm(claims, "paid ~ C(line) + age", "gamma")
sc_lift(claims$paid, m$fitted, bins = 4)
}
