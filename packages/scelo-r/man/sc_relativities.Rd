% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing.R
\name{sc_relativities}
\alias{sc_relativities}
\alias{sc_rate_table}
\title{Rating relativities from a log-link GLM}
\usage{
sc_relativities(model)

sc_rate_table(model, base = NULL)
}
\arguments{
\item{model}{A \code{scelo_glm} with a log link.}

\item{base}{Unused; kept for parity with the Python API.}
}
\value{
\code{sc_relativities()}: a \code{scelo_table} of \code{factor}, \code{level},
\code{relativity}, \code{estimate}. \code{sc_rate_table()}: a data frame with one row
per level and one column per factor.
}
\description{
exp(beta) per categorical level (base = 1) and per unit of each numeric
term; the base rate exp(intercept) is in the basis and the \code{base_rate}
attribute. \code{sc_rate_table()} is the wide view: one column per factor,
relativity per level.
}
\examples{
m <- sc_glm(sc_sample("claims"), "paid ~ C(line) + age", "gamma")
sc_relativities(m)
sc_rate_table(m)
}
