% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{sc_suggest}
\alias{sc_suggest}
\title{What the data asks for: the cleaning plan}
\usage{
sc_suggest(df)
}
\arguments{
\item{df}{A data frame.}
}
\value{
A \code{scelo_table} with \code{op}, \code{title}, \code{safe}, \code{cells}, \code{columns}, \code{why}.
}
\description{
One row per proposed cleaning op with its evidence (cells, columns, why)
and a \code{safe} flag. Safe ops are applied by \code{sc_clean(df)}; the rest only
by \code{sc_clean(df, "all")} or by naming them. Read it the way you would read
the IDE's banner.
}
\examples{
sc_suggest(sc_sample("dirty"))
}
