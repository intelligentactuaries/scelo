% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness.R
\name{sc_fairness_audit}
\alias{sc_fairness_audit}
\title{Protected-direction audit}
\usage{
sc_fairness_audit(df, pred, protected, legitimate)
}
\arguments{
\item{df}{A data frame.}

\item{pred}{Prediction column.}

\item{protected}{Protected-attribute column (numeric or 0/1).}

\item{legitimate}{Character vector of legitimate rating-factor columns.}
}
\value{
A \code{scelo_table} with rows \code{before} and \code{after}: \code{stage},
\code{alignment}, \code{disparity}, \code{fit_to_legitimate}.
}
\description{
How much of a prediction's variation beyond the legitimate factors aligns
with a protected attribute. \code{pred} is residualised on \code{legitimate} (OLS);
\code{alignment} = corr(residual, protected)^2 * var(residual) / var(pred);
\code{disparity} = standardised mean gap in \code{pred} between the protected median
split; \code{fit_to_legitimate} the R-squared of \code{pred} against its legitimate
fit. The \code{after} row replaces \code{pred} with its legitimate fit plus the
residual orthogonalised to the protected attribute; that mitigated
prediction is the \code{mitigated} attribute of the result (named by the rows
used).
}
\examples{
set.seed(1)
df <- data.frame(age = sample(18:69, 400, TRUE), prot = rbinom(400, 1, 0.3))
df$score <- 0.02 * df$age + 0.5 * df$prot + rnorm(400, 0, 0.1)
sc_fairness_audit(df, "score", "prot", "age")
}
