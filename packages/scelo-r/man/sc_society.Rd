% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swarm.R
\name{sc_society}
\alias{sc_society}
\title{Simulate a society}
\usage{
sc_society(
  scenario,
  size = 200,
  seed = NULL,
  drugs = NULL,
  population = NULL,
  concurrency = NULL,
  fresh = FALSE,
  timeout = 3600,
  progress = FALSE
)
}
\arguments{
\item{scenario}{Scenario text.}

\item{size}{Agents to simulate.}

\item{seed}{Seed (echoed by the server).}

\item{drugs}{Optional drug names.}

\item{population}{Population the sample scales to.}

\item{concurrency}{Server-side concurrency.}

\item{fresh}{Bypass the cache.}

\item{timeout}{Seconds to allow for the stream.}

\item{progress}{Print progress as agents complete.}
}
\value{
A \code{scelo_table} of agents with attributes \code{macro}, \code{macro_provenance}, \code{seed}, \code{refs}.
}
\description{
Simulate a population's response to a scenario: one row per agent (25
columns) plus the macro roll-up as attributes. \code{size} 20 to 2000 agents;
send the echoed \code{seed} back to reproduce a run exactly. Failed agents are
flagged in \code{sim_status} and excluded from the macro figures
(\code{attr(, "macro")}, with \code{attr(, "macro_provenance")}).
}
\examples{
\dontrun{
soc <- sc_society("fuel price shock in Gauteng", size = 50)
attr(soc, "macro")
}
}
