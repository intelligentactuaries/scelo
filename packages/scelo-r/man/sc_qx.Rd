% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_qx}
\alias{sc_qx}
\title{Resolve any mortality basis to an annual (age, qx) table}
\usage{
sc_qx(
  basis = NULL,
  df = NULL,
  ...,
  ages = NULL,
  age = NULL,
  qx_col = NULL,
  lx = NULL,
  deaths = NULL,
  exposure_col = NULL
)
}
\arguments{
\item{basis}{\code{NULL}, \code{"makeham"}, \code{"gompertz"}, a list of Makeham
parameters, a data frame, a named numeric vector of qx by age, or a
\code{scelo_qx} (returned as is).}

\item{df}{A data frame with an age column and qx / lx / deaths + exposure
columns (when \code{basis} is not itself the frame).}

\item{...}{Makeham overrides \code{A}, \code{B}, \code{c}, \code{multiplier}.}

\item{ages}{\code{c(from, to)} to restrict the ages (\code{NA} for an open end).}

\item{age, qx_col, lx, deaths, exposure_col}{Column names, inferred when
\code{NULL}.}
}
\value{
A \code{scelo_qx}: a list with \code{ages} (integer), \code{qx}, \code{label} and
\code{notes}; \code{as.data.frame()} gives the (age, qx) frame.
}
\description{
\code{sc_qx()} gives Scelo's illustrative Makeham from 20 to 110; \code{sc_qx(df)}
finds age + qx / lx / deaths + exposure columns; \code{sc_qx(q)} with a named
numeric vector takes qx by age; \code{sc_qx(list(A = , B = , c = ))} a custom
Makeham; \code{"gompertz"} the illustrative parameters with A = 0. Gaps in an
age range are interpolated linearly; percent-shaped qx (> 1) is divided
by 100; crude deaths / exposure rates are flagged as ungraduated.
}
\examples{
q <- sc_qx()
q$label
sc_qx(data.frame(age = c(30, 31, 33), qx = c(1.1, 1.3, 1.5)))  # percent, with a gap
}
