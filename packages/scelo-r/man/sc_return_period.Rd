% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate.R
\name{sc_return_period}
\alias{sc_return_period}
\title{Return-period losses}
\usage{
sc_return_period(
  losses,
  years = NULL,
  periods = c(2, 5, 10, 25, 50, 100, 200, 250, 500)
)
}
\arguments{
\item{losses}{One value per year (annual maxima / totals), or an event
set with \code{years} = the length of record.}

\item{years}{Years of record for an event set (\code{NULL}: one loss per year).}

\item{periods}{Return periods to tabulate.}
}
\value{
A \code{scelo_table} of \code{return_period}, \code{empirical}, \code{gumbel}.
}
\description{
From annual maxima or an event set: empirical (Weibull plotting position
(T + 1) / rank with log-linear interpolation, blank beyond the record)
and Gumbel (method of moments, only when the losses are one per year).
}
\examples{
set.seed(1)
sc_return_period(100 - 30 * log(-log(runif(40))))
}
