% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_bootstrap}
\alias{sc_bootstrap}
\title{ODP bootstrap}
\usage{
sc_bootstrap(tri, n = 1000, seed = 42, process = TRUE, average = "volume")
}
\arguments{
\item{tri}{A triangle (see \code{\link[=sc_triangle]{sc_triangle()}}), numeric matrix or long claims file.}

\item{n}{Number of simulations.}

\item{seed}{Seed for R's RNG, or \code{NULL} to continue the session's stream.}

\item{process}{Add gamma process error (\code{TRUE}) or estimation error only.}

\item{average}{\code{"volume"} (weighted), \code{"simple"} or \code{"regression"} link ratios.}
}
\value{
A \link{scelo_reserving} result (method \code{"bootstrap"}).
}
\description{
England & Verrall: resample the scaled Pearson residuals of the
incremental over-dispersed Poisson fit, refit each pseudo-triangle by
chain ladder, project, and add gamma process error. Returns the mean
reserve with SE and the p5 / p50 / p75 / p95 / p99 quantiles of the total
in \code{detail} (and the simulated totals in \code{detail$totals}, per origin in
\code{detail$by_origin}). Uses R's RNG: \code{seed} makes a run reproducible (the
session's RNG state is restored afterwards).
}
\examples{
sc_bootstrap(sc_triangle(sc_sample("claims")), n = 200, seed = 1)
}
