% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate.R
\name{sc_ensemble}
\alias{sc_ensemble}
\title{Reanalysis ensemble summary}
\usage{
sc_ensemble(df, variable = NULL, members = NULL, date = NULL)
}
\arguments{
\item{df}{A data frame (one row per date).}

\item{variable}{Column-name prefix selecting one variable ("t2m").}

\item{members}{Member columns, when not inferred.}

\item{date}{Date column (inferred by alias; optional).}
}
\value{
A \code{scelo_table} of the date, \code{mean}, \code{spread}, \code{min}, \code{max}.
}
\description{
Per-date ensemble mean, spread (sd) and range across reanalysis members:
the numeric columns whose names mention a reanalysis (era5, merra2,
jra3q, ncep, cfsr, nora), filtered by a \code{variable} prefix, or the
\code{members} given.
}
\examples{
sc_ensemble(sc_sample("climate"), "t2m")
}
