% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines.R
\name{sc_experience}
\alias{sc_experience}
\title{Mortality experience study in one line}
\usage{
sc_experience(
  data,
  expected = NULL,
  h = 100,
  band = 5,
  age = NULL,
  deaths = NULL,
  exposure = NULL
)
}
\arguments{
\item{data}{A data frame or a file path.}

\item{expected}{The expected basis (see \code{\link[=sc_ae]{sc_ae()}}); the illustrative Makeham when \code{NULL}.}

\item{h}{Whittaker-Henderson smoothing parameter.}

\item{band}{Age-band width.}

\item{age, deaths, exposure}{Column names, inferred when \code{NULL}.}
}
\value{
A \code{scelo_table} (the A/E table) with attributes \code{graduated} and \code{life_table}.
}
\description{
A/E by age band against a basis, Whittaker-Henderson graduated crude
rates, and the resulting life table. \code{data} needs age + deaths + exposure
columns (inferred, or pass \verb{age = }, \verb{deaths = }, \verb{exposure = }). Returns
the A/E table with the graduated qx in \code{attr(, "graduated")} and the life
table in \code{attr(, "life_table")}.
}
\examples{
x <- data.frame(age = rep(40:79, each = 2), deaths = rep(c(1, 2), 40), exposure = 500)
e <- sc_experience(x)
attr(e, "life_table")$ex[1]
}
