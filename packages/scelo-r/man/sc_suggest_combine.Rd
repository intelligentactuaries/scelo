% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{sc_suggest_combine}
\alias{sc_suggest_combine}
\title{The IDE's combine suggestion}
\usage{
sc_suggest_combine(base, other)
}
\arguments{
\item{base, other}{Two data frames.}
}
\value{
A one-row \code{scelo_table}: \code{strategy}, \code{key}, \code{right_key},
\code{confidence}, \code{dedupe_exact}, \code{schema_overlap}, \code{shared_columns},
\code{candidate_keys}.
}
\description{
Strategy (\code{append} / \code{join-left}), key, schema overlap and confidence
for combining two frames, with the candidate keys in \code{attr(, "keys")}.
}
\examples{
a <- data.frame(policy_id = c("P1", "P2", "P3"), premium = 1:3)
b <- data.frame(Policy_ID = c("P1", "P2", "P2", "P4"), claims = 0:3, premium = 9)
sc_suggest_combine(a, b)
}
