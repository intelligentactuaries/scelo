% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{sc_diff}
\alias{sc_diff}
\title{Cells that differ between two frames}
\usage{
sc_diff(a, b, key = NULL, tol = 0)
}
\arguments{
\item{a, b}{Two data frames.}

\item{key}{Column to align on (\code{NULL}: by position, the key is then the row number).}

\item{tol}{Absolute tolerance for numeric columns.}
}
\value{
A \code{scelo_table} with \code{key}, \code{column}, \code{a}, \code{b}, \code{delta}.
}
\description{
Rows are aligned on \code{key} (or by position) and the shared columns
compared: numeric columns within \code{tol}, everything else as text; \code{NA}
against \code{NA} is not a difference.
}
\examples{
a <- data.frame(policy_id = c("P1", "P2", "P3"), premium = 1:3)
sc_diff(a, data.frame(policy_id = c("P1", "P2", "P3"), premium = c(1, 2, 5)), key = "policy_id")
}
