% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workspace.R
\name{sc_bottleneck}
\alias{sc_bottleneck}
\title{Workspace bottleneck}
\usage{
sc_bottleneck(df, columns = NULL, r = 3, l1 = 0.001, max_rows = 20000)
}
\arguments{
\item{df}{A data frame.}

\item{columns}{Driver columns (all numeric columns by default).}

\item{r}{Number of codes.}

\item{l1}{L1 penalty of the projected-gradient fit.}

\item{max_rows}{Thin the frame to at most this many rows.}
}
\value{
A \code{scelo_table} with a \code{head} column and one \verb{code k: name} column
per code; attributes \code{participation_ratio}, \code{reconstruction_r2},
\code{causal_alignment}, \code{sparsity}, \code{code_loadings} (a data frame of the
oriented eigenvectors), \code{eigenvalues}.
}
\description{
\code{r} codes (top eigen-directions of the standardised drivers) broadcast
non-negatively to every column. Returns the heads x codes broadcast
matrix with the code names, and as attributes the participation ratio,
reconstruction R^2, causal alignment and sparsity (the IDE's four
workspace metrics).
}
\examples{
ws <- sc_sample("workspace-demo")
b <- sc_bottleneck(ws[, 1:6], r = 2)
attr(b, "causal_alignment")
}
