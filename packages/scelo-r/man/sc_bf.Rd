% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{sc_bf}
\alias{sc_bf}
\title{Bornhuetter–Ferguson}
\usage{
sc_bf(
  tri,
  apriori = NULL,
  premium = NULL,
  elr = NULL,
  average = "volume",
  tail_factor = 1
)
}
\arguments{
\item{tri}{A triangle (see \code{\link[=sc_triangle]{sc_triangle()}}), numeric matrix or long claims file.}

\item{apriori}{A number, a vector (one per origin) or a \link{scelo_reserving} result.}

\item{premium}{Premium per origin (used with \code{elr}).}

\item{elr}{Expected loss ratio applied to \code{premium}.}

\item{average}{\code{"volume"} (weighted), \code{"simple"} or \code{"regression"} link ratios.}

\item{tail_factor}{Tail factor beyond the last observed lag.}
}
\value{
A \link{scelo_reserving} result (method \code{"bornhuetter-ferguson"}).
}
\description{
Reserve = a-priori ultimate × (1 − 1/CDF). The a-priori ultimate per
origin comes from, in order: \code{apriori} (a number, a per-origin vector, or
a chain-ladder result whose ultimates seed it), \verb{premium × elr}, or the
book-average chain-ladder ultimate (the IDE's standalone default: a flat
ELR would cancel back to CL).
}
\examples{
tri <- sc_triangle(sc_sample("claims"))
sc_bf(tri, apriori = sc_chain_ladder(tri))
sc_bf(tri, apriori = 500000)
}
