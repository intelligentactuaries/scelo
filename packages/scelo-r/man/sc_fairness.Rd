% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fairness.R
\name{sc_fairness}
\alias{sc_fairness}
\title{Group fairness metrics}
\usage{
sc_fairness(df, y, pred, group, threshold = 0.5)
}
\arguments{
\item{df}{A data frame.}

\item{y}{Outcome column (0/1, logical, or yes / true strings).}

\item{pred}{Score or decision column.}

\item{group}{Protected-group column.}

\item{threshold}{Decision threshold for a score.}
}
\value{
A \code{scelo_table}, one row per group.
}
\description{
Per level of \code{group}: n, base rate, selection rate, disparate impact,
TPR / FPR, precision (calibration of the positive decision) and the mean
score, with the parity gaps in the notes. \code{pred} may be a probability /
score (thresholded at \code{threshold}) or a 0/1 decision; \code{y} a 0/1 outcome.
Disparate impact is each group's selection rate over the best-off
group's (the four-fifths rule flags < 0.8).
}
\examples{
df <- data.frame(g = rep(c("a", "b"), 50), y = rep(c(0, 1, 1, 0), 25),
                 score = rep(c(0.7, 0.4, 0.8, 0.3), 25))
sc_fairness(df, "y", "score", "g")
}
