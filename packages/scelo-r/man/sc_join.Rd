% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{sc_join}
\alias{sc_join}
\title{Join a second frame onto a base frame}
\usage{
sc_join(base, other, key = NULL, right_key = NULL, how = "left")
}
\arguments{
\item{base, other}{Two data frames.}

\item{key}{Key column in \code{base} (inferred from the shared columns when \code{NULL}).}

\item{right_key}{Key column in \code{other} (the column with the same name, case-insensitively, when \code{NULL}).}

\item{how}{\code{"left"} (default) keeps every base row; anything else is an inner join.}
}
\value{
A \code{scelo_table}.
}
\description{
Joins \code{other} onto \code{base} on \code{key} (inferred when omitted): the key is
matched as text, the first right match wins (a join never multiplies
rows), clashing column names are renamed \verb{_2}, \verb{_3} ...
}
\examples{
a <- data.frame(policy_id = c("P1", "P2", "P3"), premium = 1:3)
b <- data.frame(Policy_ID = c("P1", "P2", "P2", "P4"), claims = 0:3, premium = 9)
sc_join(a, b)
}
