% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifelib.R
\name{sc_normalise_model_points}
\alias{sc_normalise_model_points}
\title{Map a policy file onto lifelib's model-point columns}
\usage{
sc_normalise_model_points(df)
}
\arguments{
\item{df}{A policy file.}
}
\value{
A data frame with \code{policy_id} first.
}
\description{
\code{policy_id}, \code{age_at_entry}, \code{sex}, \code{sum_assured}, \code{policy_term},
\code{duration_mth}, \code{premium_pp}, \code{policy_count} (plus \code{account_value} and
\code{product} when present), found by exact lower-case alias. Sex becomes
M / F; duration defaults to 0 and policy_count to 1; rows missing age /
sum assured / term (or with non-positive values) are dropped; duplicate
ids get a \verb{#k} suffix.
}
\examples{
head(sc_normalise_model_points(sc_sample("lifelib-mp")))
}
