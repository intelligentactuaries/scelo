% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/life.R
\name{sc_scr_life}
\alias{sc_scr_life}
\title{Solvency II life SCR on the BasicTerm projection}
\usage{
sc_scr_life(mp, assumptions = NULL, shocks = NULL, corr = NULL)
}
\arguments{
\item{mp}{A model-point file.}

\item{assumptions}{A list as from \code{\link[=sc_basicterm_assumptions]{sc_basicterm_assumptions()}}.}

\item{shocks}{Named overrides of \link{SC_SII_LIFE_SHOCKS}.}

\item{corr}{Correlation matrix (default \link{SC_SII_LIFE_CORR}).}
}
\value{
A \code{scelo_table} (the \code{\link[=sc_aggregate_scr]{sc_aggregate_scr()}} table) with attributes
\code{scr}, \code{bel}, \code{charges}, \code{lapse}, \code{shocks}.
}
\description{
Each sub-risk re-runs the projection under its standard-formula shock
(mortality +15 \%, longevity -20 \%, lapse up / down 50 \% and a 40 \% mass
lapse (the worst of the three counts), expenses +10 \% with 1 \% inflation,
CAT +0.15 \% mortality in year 1) and takes the increase in the
best-estimate liability (PV outflows - PV premiums), floored at zero; the
charges are aggregated with the standard-formula life correlation matrix.
Disability and revision are not modelled for term business and enter as
zero.
}
\examples{
s <- sc_scr_life(sc_sample("lifelib-mp"))
attr(s, "scr")
}
