% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{sc_aggregate_scr}
\alias{sc_aggregate_scr}
\title{Square-root capital aggregation}
\usage{
sc_aggregate_scr(modules, corr = NULL)
}
\arguments{
\item{modules}{Named numeric vector (or named list) of capital charges.}

\item{corr}{Correlation matrix (or data frame) with module names as
dimnames; \code{NULL} for \link{SC_SII_LIFE_CORR}.}
}
\value{
A \code{scelo_table} with \code{module}, \code{charge}, \code{marginal}, \code{share} and
the rows \code{sum}, \code{SCR}, \code{diversification}.
}
\description{
SCR = sqrt(v' rho v) of module capital charges with a correlation matrix
(default: the SII life matrix). Modules absent from the matrix are treated
as uncorrelated with the rest (rho = 0); the table shows each module's
charge, its marginal (Euler) contribution v_i * (rho v)_i / SCR and the
diversification benefit.
}
\examples{
sc_aggregate_scr(c(mortality = 100, longevity = 50, lapse = 200, expense = 80, cat = 40))
}
