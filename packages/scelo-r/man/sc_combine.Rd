% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine.R
\name{sc_combine}
\alias{sc_combine}
\title{Combine datasets the way the IDE does}
\usage{
sc_combine(base, ..., key = NULL, how = NULL)
}
\arguments{
\item{base}{The first data frame.}

\item{...}{Further data frames, combined one at a time.}

\item{key}{Join key to force.}

\item{how}{\code{"append"}, \code{"join"} / \code{"join-left"} / \code{"left"}, \code{"inner"} /
\code{"join-inner"}, or \code{NULL} to let each step be suggested.}
}
\value{
A \code{scelo_table} whose notes record each step.
}
\description{
Each step is suggested (append or join-left, see \code{\link[=sc_suggest_combine]{sc_suggest_combine()}})
unless \code{how} / \code{key} force it.
}
\examples{
a <- data.frame(policy_id = c("P1", "P2", "P3"), premium = 1:3)
sc_combine(a, data.frame(policy_id = c("P1", "P2", "P3"), premium = c(1, 2, 5)))
}
