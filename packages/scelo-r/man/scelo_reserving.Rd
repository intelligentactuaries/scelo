% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reserving.R
\name{scelo_reserving}
\alias{scelo_reserving}
\alias{print.scelo_reserving}
\alias{summary.scelo_reserving}
\title{Reserving results}
\usage{
\method{print}{scelo_reserving}(x, ...)

\method{summary}{scelo_reserving}(object, ...)
}
\arguments{
\item{x, object}{A \code{scelo_reserving} result.}

\item{...}{Passed on to the table's print method; ignored by \code{summary()}.}
}
\value{
\code{print()} returns \code{x} invisibly; \code{summary()} a one-row data frame.
}
\description{
Every reserving method returns a \code{scelo_reserving} list: \code{method}, \code{table}
(the per-origin \code{scelo_table} with an \code{origin} column and a \code{total} row),
\code{ibnr}, \code{ultimate}, \code{latest}, \code{factors}, \code{cdf}, \code{se}, \code{cv} and \code{detail}.
Printing shows the headline line then the table; \code{summary()} gives a
one-row data frame (method, latest, ultimate, ibnr and, where the method
has one, se and cv).
}
\examples{
cl <- sc_chain_ladder(sc_sample("claims"))
cl
summary(cl)
}
