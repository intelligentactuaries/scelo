% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wmtr.R
\name{sc_wmtr_params}
\alias{sc_wmtr_params}
\alias{SC_DEFAULT_WMTR_PARAMS}
\alias{print.scelo_wmtr_params}
\title{WMTR forecast parameters}
\format{
\code{SC_DEFAULT_WMTR_PARAMS}: the engine defaults, a \code{scelo_wmtr_params}.
}
\usage{
sc_wmtr_params(
  population = 500,
  sqftPerResident = 300,
  alphaM = 0.4,
  alphaT = 0.3,
  alphaR = 0.3,
  wF = 0.4,
  wRel = 0.3,
  wS = 0.3,
  pProduction = 0.4,
  pFamily = 0.25,
  pReligion = 0.15,
  pSpatial = 0.1,
  pLeisure = 0.1,
  initFamily = 0.7,
  initReligion = 0.6,
  shock = "moderate",
  collapse = 0.3,
  recovery = 5,
  growth = 0.2,
  stability = 0.1,
  horizon = 30,
  nPaths = 200,
  seed = 42,
  thresholds = NULL
)

SC_DEFAULT_WMTR_PARAMS

\method{print}{scelo_wmtr_params}(x, ...)
}
\arguments{
\item{population}{Residents (informational).}

\item{sqftPerResident}{Floor space per resident, the spatial input to R.}

\item{alphaM, alphaT, alphaR}{Cobb-Douglas exponents of material, time and relational capital (normalised to sum to 1 when run).}

\item{wF, wRel, wS}{Weights of family, religion and space inside R.}

\item{pProduction, pFamily, pReligion, pSpatial, pLeisure}{Time allocation (normalised to sum to 1 when run).}

\item{initFamily, initReligion}{Initial family and religion capital in \verb{[0, 1]}.}

\item{shock}{\code{"mild"}, \code{"moderate"} or \code{"severe"} (see \link{SC_SHOCK_PARAMS}).}

\item{collapse, recovery, growth, stability}{Outcome thresholds: collapsed below \code{collapse * W0} for \code{recovery} consecutive years; grew above \code{1 + growth}; declined below \code{1 - stability}.}

\item{horizon}{Years simulated.}

\item{nPaths}{Monte Carlo paths.}

\item{seed}{Mulberry32 seed.}

\item{thresholds}{Optional list with \code{collapse}, \code{recovery}, \code{growth}, \code{stability} (overrides the four scalars).}

\item{x}{A \code{scelo_wmtr_params} to print.}

\item{...}{Ignored.}
}
\value{
A list of class \code{scelo_wmtr_params}.
}
\description{
Single-community forecast parameters (\code{DEFAULT_WMTR_SINGLE_PARAMS} in
the engine). Every argument has the engine's default; \code{thresholds} may
carry the four outcome thresholds as one list (the shape the swarm's
\verb{/api/wmtr} config uses).
}
\examples{
sc_wmtr_params(shock = "severe", horizon = 10)
}
